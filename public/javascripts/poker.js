define(['worker!ww/tournamentGameThread'], function() {

    var startGame = function(players, structure) {
        // randomize  
	};

	var startGameasdf = function(players, structure) {
        // randomize  
	};

    return {
        startGame: startGame 
    };


});